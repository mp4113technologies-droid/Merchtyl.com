# Public origin of the deployed Spring Boot API, without a trailing slash.
# Leave empty for local development through the Vite /api proxy.
VITE_API_BASE_URL=https://api.merchtyl.com
VITE_PUBLIC_BASE_DOMAIN=merchtyl.com
